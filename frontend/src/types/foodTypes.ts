export type FridgeItem = {
    id: number;
    name: string;
    category: string;
    quantity: number;
    unit: string;
    expirationDate: string;
};

export type GroceryItem = {
    id: number,
    name: string,
    category: string,
    quantity: number,
    unit: string,
    expirationDate: string,
};
export type FridgeItem = {
    id: number;
    name: string;
    category: string;
    quantity: number;
    unit: string;
    expiration_date: string;
};

export type GroceryItem = {
    id: number,
    name: string,
    category: string,
    quantity: number,
    unit: string,
    expiration_date: string,
};
from sqlmodel import Session
from models.models import FridgeItem
import os


#Code to ensure when button "add new item in fridge" is pushed
# a form appears to which lets the user fill in details
def add_fridge_item(table_name: str):
    # somehow get userinput from form and store it in variables
    pass